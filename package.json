{
  "name": "malone.io",
  "devDependencies": {
    "roots": "^3.1.0",
    "surge": "^0.17.7"
  },
  "dependencies": {
    "autoprefixer-stylus": "0.8.x",
    "coffee-script": "^1.0.0",
    "css-pipeline": "0.3.x",
    "jade": "^1.0.0",
    "js-pipeline": "0.2.x",
    "jstransformer-marked": "^1.0.0",
    "marked": "0.3.x",
    "rupture": "0.6.x",
    "stylus": "0.52.x"
  },
  "scripts": {
    "test": "echo \"No tests configured.\"",
    "compile": "roots compile -e production",
    "deploy": "npm run compile && surge --project ./public --domain malone.io"
  }
}
